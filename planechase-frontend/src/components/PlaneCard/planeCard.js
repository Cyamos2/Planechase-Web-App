import React from 'react';

function PlaneCard({ cardText }) {
  return (
    <div className="PlaneCard">
      <h2>Planar Card</h2>
      <p>{cardText}</p>
    </div>
  );
}

export default PlaneCard;
